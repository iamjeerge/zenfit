[**ZenFit API Reference v1.0.0**](../../../README.md)

***

[ZenFit API Reference](../../../modules.md) / [theme/colors](../README.md) / Colors

# Variable: Colors

> `const` **Colors**: `object`

Defined in: [theme/colors.ts:13](https://github.com/iamjeerge/zenfit/blob/main/src/theme/colors.ts#L13)

## Type Declaration

### background

> **background**: `string` = `"#0D0B1A"`

### backgroundLight

> **backgroundLight**: `string` = `"#151326"`

### card

> **card**: `string` = `"#1A1730"`

### cardHover

> **cardHover**: `string` = `"#221F3A"`

### cosmicBlue

> **cosmicBlue**: `string` = `"#60A5FA"`

### error

> **error**: `string` = `"#F87171"`

### glassBackground

> **glassBackground**: `string` = `"rgba(26, 23, 48, 0.6)"`

### glassBackgroundLight

> **glassBackgroundLight**: `string` = `"rgba(26, 23, 48, 0.4)"`

### glassBorder

> **glassBorder**: `string` = `"rgba(196, 181, 253, 0.2)"`

### heartCardio

> **heartCardio**: `string` = `"#FBBF24"`

### heartExtreme

> **heartExtreme**: `string` = `"#EF4444"`

### heartFatBurn

> **heartFatBurn**: `string` = `"#60A5FA"`

### heartPeak

> **heartPeak**: `string` = `"#F87171"`

### heartRest

> **heartRest**: `string` = `"#34D399"`

### info

> **info**: `string` = `"#60A5FA"`

### lavender

> **lavender**: `string` = `"#C4B5FD"`

### moonlight

> **moonlight**: `string` = `"#F1F5F9"`

### overlay

> **overlay**: `string` = `"rgba(13, 11, 26, 0.7)"`

### overlayLight

> **overlayLight**: `string` = `"rgba(13, 11, 26, 0.4)"`

### rosePetal

> **rosePetal**: `string` = `"#F472B6"`

### rosePetalLight

> **rosePetalLight**: `string` = `"#FBCFE8"`

### sacredGold

> **sacredGold**: `string` = `"#FBBF24"`

### sageLeaf

> **sageLeaf**: `string` = `"#34D399"`

### success

> **success**: `string` = `"#34D399"`

### sunriseOrange

> **sunriseOrange**: `string` = `"#FB923C"`

### textAccent

> **textAccent**: `string` = `"#C4B5FD"`

### textMuted

> **textMuted**: `string` = `"#64748B"`

### textPrimary

> **textPrimary**: `string` = `"#F8FAFC"`

### textSecondary

> **textSecondary**: `string` = `"#94A3B8"`

### violet

> **violet**: `string` = `"#7C3AED"`

### violetLight

> **violetLight**: `string` = `"#A78BFA"`

### warning

> **warning**: `string` = `"#FBBF24"`
